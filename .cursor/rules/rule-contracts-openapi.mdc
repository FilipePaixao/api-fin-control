---
description: OpenAPI contract — service.yaml aligned with HTTP API
alwaysApply: true
---

# Contracts (`src/contracts`)

- Main specification: [src/contracts/service.yaml](src/contracts/service.yaml).
- Any change to **path**, method, request/response, or documented codes on the exposed API must be reflected in the YAML.
- Keep consistency with controllers and real routes in `src/application`.
