---
description: Infraestructure layer — Mongo IM*, adapters, repos, messaging
globs: src/infraestructure/**/*
alwaysApply: false
---
# Infraestructure (`src/infraestructure`)

- Mongo models: interface **`IM*`** extends domain `I*` + `_id`, `updatedAt`, etc. (see AGENTS.md).
- Schema in `db/mongo/schema/`, model in `db/mongo/models/`, `IM*` interface in `db/mongo/interfaces/`.
- Concrete repositories: **thin persistence layer** — implement domain contracts; use pure **adapters**:
  - `dbToInternal(IM*) → I*`
  - `internalToDb(I*)` with no side effects
- **Updates:** persist what the service sent — `$set: updateData` / `$set: payload`. Reference: [`expense.repository.write.ts`](mdc:src/infraestructure/repository/expense/expense.repository.write.ts).
- **Forbidden in repository:**
  - business rules (404/409, uniqueness, state transitions, instantiate `*ServiceEntity`)
  - knowing nested domain shape to merge/flatten (e.g. `profile.*` dot-notation loops)
  - product defaults / fallbacks (`x?.length ? x : undefined`, `field || metadata.field`)
  - inventing pagination semantics (“últimas N”) — expose skip/limit/sort; service decides
- Return `null` if not found; **service** throws `RESOURCE_NOT_FOUND` / `RESOURCE_CONFLICT`.
- **Allowed:** CRUD/query, mapping, `try/catch` → `DATABASE_ERROR` + log.
- ❌/✅ examples: [rule.business-rules-layers.mdc](rule.business-rules-layers.mdc).
- Kafka (optional scaffolding — no `messaging/` under `src/infraestructure` yet): when added, put implementations in `messaging/<event>/`; contracts stay in **domain**.
